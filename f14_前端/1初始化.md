
# 安装 nodejs npm pnpm
sudo apt update
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
sudo npm install -g pnpm

node --version
npm --version
pnpm --version

# vite 创建 react 项目
pnpm create vite aaa

# 初始化
cd aaa
pnpm install

# 启动
pnpm run dev

# 开发
只需要修改src文件夹内部就可以